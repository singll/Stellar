import Root, { type ButtonSize, type ButtonVariant, getButtonClasses } from './button.svelte';

export { Root as Button, getButtonClasses, type ButtonSize, type ButtonVariant };

export default Root;
