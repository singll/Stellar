import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	getButtonClasses
} from './button.svelte';

export {
	Root,
	Root as Button,
	getButtonClasses,
	type ButtonSize,
	type ButtonVariant,
	type ButtonProps
};

export default Root;
